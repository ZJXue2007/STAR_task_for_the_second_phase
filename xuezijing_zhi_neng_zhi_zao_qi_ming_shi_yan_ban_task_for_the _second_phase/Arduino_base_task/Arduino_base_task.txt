#include <Servo.h>; 
 Servo myservo;  //设置一个控制舵机的对象
 
void setup() {
 myservo.attach(9); //让引脚9控制舵机
}
void loop() {
 int i;
 int angle[]={0,30,45,90,135};//一个装着角度大小的数组
 for(i=0;i<5;i++){ //通过i的增加和重复循环让舵机改变转动角度并重复执行
 myservo.write(angle[i]);              
 delay(2000); 
 }
}